import { KspPartnerPipe } from './ksp-partner.pipe';

describe('KspPartnerPipe', () => {
  it('create an instance', () => {
    const pipe = new KspPartnerPipe();
    expect(pipe).toBeTruthy();
  });
});
